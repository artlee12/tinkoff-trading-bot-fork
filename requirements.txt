# Note: tinkoff-investments must be installed separately or via system Python 3.12
# It requires Python 3.12 specifically and may not be available in standard pip
# Setup script will handle this automatically

pydantic>=1.9,<2.0
pydantic[dotenv]>=1.9,<2.0
python-dateutil>=2.8
deprecation>=2.1
numpy>=1.22
pytest>=7.0
pytest-mock>=3.7
pytest-asyncio>=0.18
grpcio>=1.50
protobuf>=4.0,<5.0
